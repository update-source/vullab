const authController = require("./auth.controller");
const jwtController = require("./jwt.controller");
const xmlController = require("./xml.controller");
const profileController = require("./profile.controller");

module.exports = {
  authController,
  jwtController,
  xmlController,
  profileController,
};
